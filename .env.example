# =============================================================
# fittracker-dev (mobile-only)
# =============================================================

# IP da máquina host na LAN (para o celular conectar via Expo Go)
# Descobrir com: hostname -I | awk '{print $1}'
LAN_IP=192.168.1.100
